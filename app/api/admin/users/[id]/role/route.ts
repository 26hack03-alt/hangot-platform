import { findUserById,updateUserRole } from "../../../../../lib/database/users";
import { writeAudit } from "../../../../../lib/database/audit";
import { id,requireUser,sameOrigin } from "../../../../../lib/security";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const auth=await requireUser(["admin"]);if("error" in auth)return auth.error;if(!sameOrigin(request))return Response.json({error:"INVALID_ORIGIN"},{status:403});const body=await request.json().catch(()=>null);if(!body||!["student","teacher"].includes(body.role))return Response.json({error:"INVALID_ROLE"},{status:400});const userId=(await params).id,target=await findUserById(userId);if(!target)return Response.json({error:"NOT_FOUND"},{status:404});if(target.role==="admin")return Response.json({error:"ADMIN_ROLE_PROTECTED"},{status:409});const role=body.role==="teacher"?"club_manager":"student";await updateUserRole(userId,role);await writeAudit({id:id("audit"),actor_user_id:auth.user.id,actor_role:"admin",action_type:"teacher.role_changed",target_type:"user",target_id:userId,before_data:{role:target.role},after_data:{role},created_at:new Date().toISOString()});return Response.json({user:{id:userId,alias:target.alias,role:body.role,isActive:target.isActive}})}
