# 새롬온 설계 제안

## 1. DB 설계

- `users`: 학교 계정 식별자, 암호화 이름/학번, 역할
- `club_assignments`: 동아리별 학생부장·지도교사 배정
- `applications`: 신청, 지원동기 암호문, 상태, 블라인드 검토 여부
- `reviews`: 학생부장 추천·대기·불합격 의견
- `approvals`: 지도교사의 최종 승인
- `recommendation_history`: 로그인 사용자의 추천 기록(보존기한 포함)
- `notifications`, `audit_logs`: 알림과 민감 작업 감사 이력
- Supabase를 신청·권한·운영 데이터의 원본으로 사용한다. 검증된 로컬 동아리 목록을 기본으로 사용하며, 선택적으로 공개 Spreadsheet CSV를 읽고 실패 시 로컬 목록으로 대체한다.

개인정보 필드는 애플리케이션 계층에서 암호화하고, 조회 시 역할과 담당 동아리를 함께 검사한다. 학번·이름 검색용 평문 인덱스는 두지 않는다.

## 2. API 설계

- `GET /api/clubs`: 공개·visible 동아리 목록(Spreadsheet CSV 연동, 5분 캐시)
- `GET /api/clubs/:id`: 공개 상세
- `POST /api/recommendations`: 입력 최소화, 공개 동아리 후보 검색, 구조화된 추천 생성
- `GET/POST/DELETE /api/applications`: 본인 신청 조회·생성·취소
- `GET/PATCH /api/reviews/:clubId`: 담당 학생부장 평가
- `GET/PATCH /api/approvals/:clubId`: 담당 지도교사 최종 결정
- `POST /api/admin/sync`: Supabase의 최신 신청 스냅샷을 운영용 Google Sheet에 수동 반영
- `PATCH /api/admin/clubs/:id`: 모집 기간·공개 여부 등 운영 메타데이터 관리

모든 쓰기 API는 학교 계정 인증, CSRF 방어, 서버 측 RBAC, 감사 로그를 적용한다.

## 3. 화면 설계

- 공개: 홈/통합 검색/분야 필터 → 동아리 상세·포스터 → AI 추천
- 학생: 신청서 → 완료 → 내 신청 현황·추천 기록
- 학생부장: 담당 동아리 → 블라인드 신청자 목록 → 의견 작성
- 지도교사: 담당 동아리 → 학생부장 의견 → 최종 승인·모집 상태·정보 수정
- 관리자: Supabase 운영 관리 → 필요 시 Google Sheets 운영 사본 수동 동기화 → 통계

모바일에서는 하단 내비게이션, 데스크톱에서는 상단 내비게이션을 사용한다.

## 4. 권한 설계

| 행동 | 비로그인 | 학생 | 학생부장 | 지도교사 | 관리자 |
|---|---:|---:|---:|---:|---:|
| 공개 동아리 열람·검색·AI 추천 | 허용 | 허용 | 허용 | 허용 | 허용 |
| 신청·취소·본인 현황 | - | 본인 | 본인 | 본인 | 운영 조회 |
| 지원동기 평가 | - | - | 담당 동아리 | 조회 | 감사 |
| 최종 승인·모집 상태 | - | - | - | 담당 동아리 | 전체 |
| 동아리 생성·담당자 지정 | - | - | - | - | 전체 |

학생부장 API 응답은 `blind_review=true`일 때 이름·학번을 내려주지 않는다. UI에서 숨기는 방식만으로 처리하지 않는다.

## 5. AI 추천 로직

1. 관심 분야·희망 진로·선호/비선호 활동을 짧은 구조화 입력으로 받는다.
2. 검증된 공개 동아리 데이터만 임베딩/키워드 하이브리드 검색한다.
3. 모집 상태, 분야·진로 일치, 선호 활동 일치, 비선호 활동 충돌을 점수화한다.
4. 상위 후보만 LLM에 제공해 추천 이유·관련 진로·유사 후보를 JSON으로 생성한다.
5. 동아리 정보 밖의 내용을 만들지 않도록 club_id 기반 근거를 강제한다.
6. 비로그인 입력은 응답 후 폐기하고, 로그인 기록은 동의 시 암호화·기한부 저장한다.

## 6. 폴더 구조

```text
app/
  api/clubs/          # 선택적 공개 CSV와 로컬 fallback
  page.tsx            # 공개 탐색과 추천 UI
db/schema.ts          # 신청·평가·승인·권한·로그
drizzle/              # D1 마이그레이션
public/               # 포스터와 데모용 시트 형식 데이터
worker/               # Cloudflare 런타임 진입점
```

운영 확장 시 `activities` 공통 도메인을 만들고 `club`, `event`, `volunteer`, `student_council`, `career_program`을 activity type으로 분리한다.

## 7. 개발 순서

1. 공개 동아리 데이터 검증·카탈로그
2. 학교 계정 인증과 역할/담당 범위
3. 신청·취소·현황과 개인정보 암호화
4. 블라인드 평가와 지도교사 최종 승인
5. 근거 기반 AI 추천과 기록 정책
6. 관리자 기간·담당자·통계와 수동 Sheets 사본 동기화
7. 접근성·보안·부하·감사 로그 검증 후 시범 운영
