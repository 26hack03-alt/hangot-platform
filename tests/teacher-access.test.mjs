import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";const root=new URL("../",import.meta.url),read=p=>readFile(new URL(p,root),"utf8");
test("teacher role and assignment constraints remain",async()=>{const[sql,security,header]=await Promise.all([read("supabase/migrations/202608060001_vercel_postgres_schema.sql"),read("app/lib/security.ts"),read("app/components/HeaderSession.tsx")]);assert.match(sql,/student', 'club_manager', 'admin/);assert.match(sql,/teacher_clubs_teacher_club_unique/);assert.match(security,/"student" \| "club_manager" \| "admin"/);assert.match(header,/user\.role === "club_manager"/)});
test("admin teacher APIs stay protected and private",async()=>{const sources=await Promise.all([read("app/api/admin/teachers/route.ts"),read("app/api/admin/users/[id]/role/route.ts"),read("app/api/admin/teacher-assignments/route.ts"),read("app/api/admin/teacher-assignments/[id]/route.ts")]);for(const s of sources){assert.match(s,/requireUser\(\["admin"\]\)/);assert.doesNotMatch(s,/authUserId|recoveryHash|email|googleId/i)}assert.match(sources[1],/ADMIN_ROLE_PROTECTED/);assert.match(sources[2],/DUPLICATE_ASSIGNMENT/)});
test("teacher application scope comes only from session assignments",async()=>{const[list,detail,status,rpc]=await Promise.all([read("app/api/teacher/applications/route.ts"),read("app/api/teacher/applications/[id]/route.ts"),read("app/api/teacher/applications/[id]/status/route.ts"),read("supabase/migrations/202608060002_rls_and_application_rpc.sql")]);for(const s of[list,detail,status]){assert.match(s,/requireUser\(\["club_manager"\]\)/);assert.match(s,/teacherClubIds\(auth\.user\.id\)/);assert.doesNotMatch(s,/teacherUserId.*(?:params|body)/)}assert.match(status,/actorRole:"teacher"/);assert.match(rpc,/TEACHER_SCOPE_REQUIRED/)});
